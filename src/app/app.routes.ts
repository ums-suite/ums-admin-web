import { Routes } from '@angular/router';

/**
 * Root route table. Fleshed out in ADMIN-6 (routing shell + 15-module navigation IA); ADMIN-1
 * only needs the app to bootstrap and build cleanly.
 */
export const routes: Routes = [];
